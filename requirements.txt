argon2-cffi==20.1.0
itsdangerous==1.1.0
passlib==1.7.4
pydantic==1.7.3
pylint==2.6.0
pylint-sqlalchemy==0.2.0
SQLAlchemy==1.3.22
starlette==0.14.1
uvicorn==0.13.3

# comment this line out upon reading
READ-THE-REQUIREMENTS-TXT
# @requires is broken until starlette 0.13.8
# fastapi latest is on starlette 0.13.6 and has not merged the bump for some reason
# pip is LITERALLY USELESS so run this command in order to install fastapi with starlette bump: 
# pip3 install git+https://github.com/hanneskuettner/fastapi.git@bump-starlette
